class Sample(


) {
    fun sample() {


        listOf(

        )

        listOf() // named argument

        val sample = Sample() // constructor argument

        val b = "enabled"

        val c = "enabled"

        // conditions in when expression
        val d = when ("a") {
            else -> "b"
        }

        val e = when ("a") {

            else -> "b"
        }

        val f = when ("a") {


            else -> "b"
        }

        val g = when ("a") {
            is String -> {

            }

            else -> "b"
        }
    }


    class Sample2
}
