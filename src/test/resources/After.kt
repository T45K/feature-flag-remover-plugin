// @formatter:off
class Sample(



) {
    fun sample() {




        listOf(

        )

        listOf() // named argument

        val sample = Sample() // constructor argument

        val b = "enabled"

        val c = "enabled"

        // conditions in when expression
        val d = when ("a") {
            -> "a"
            else -> "b"
        }

        val e = when ("a") {
            is String ->
            else -> "b"
        }

        val f = when ("a") {
            is String ->

            else -> "b"
        }

        val g = when ("a") {
            is String -> {

            }

            else -> "b"
        }
    }



    class Sample2
}
